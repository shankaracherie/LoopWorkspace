name: Keep Nightscout Awake

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Nightscout
        run: curl -s https://cgm-remote-monitor-4myw.onrender.com/api/v1/status.json
